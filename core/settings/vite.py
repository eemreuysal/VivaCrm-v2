"""
Django Vite integration settings
"""
from pathlib import Path
from .base import BASE_DIR, DEBUG

# Django Vite settings - DISABLED
# We're using traditional static files approach instead

# This file is kept for reference but the settings are not used